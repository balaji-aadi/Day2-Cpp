#include <bits/stdc++.h> 
int findDuplicate(vector<int> &arr, int n){
    int ans = -1;
	unordered_map<int,int> m;
     for(int i=0;  i<n; i++){
         m[arr[i]]++;
     }
    
    
    for(auto it: m){
        int key = it.first;
        int value = it.second;
        if(value > 1){
            ans = key;
        }
    }
    return ans;
}
